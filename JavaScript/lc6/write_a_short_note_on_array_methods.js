1.push

    => The push() method adds new items to the end of an array.The push() method changes the length of the array.

        ex

let num = [1, 2, 3];
num.push(4);
console.log(num);


2.pop

    => JavaScript Array pop() Method.JavaScript Array pop() Method is used to remove the last element of the array and also 
       returns the removed element.This function decreases the length of the array.

    ex

let numbers = [1, 2, 3, 4, 5];
let number = numbers.pop(5);
console.log(numbers);


3.length

    => length is a property of JavaScript strings, arrays, and some other objects that returns the number of characters or elements
        in that object.For example, if you have a string 'hello', calling the.length property will return 5 because the string 'hello' 
        has 5 characters.

    ex

    let numbers = [1, 2, 3, 4, 5];
    console.log(numbers.length);




